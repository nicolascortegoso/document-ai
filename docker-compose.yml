services:
  test:
    build:
      context: .
      target: test
    volumes:
      - .:/app
    command: sh -c "ruff check . && pytest --cov=common --cov=libs --cov=backends --cov=pipelines --cov-report=term-missing"